# Set the default behavior, in case core.autocrlf is not set.
* text=auto

# Declare files to always be normalized and converted on checkout.
*.ino text
*.tex text

# Declare files that will always have defined line endings on checkout.
#*.ino text eol=lf
#*.tex text eol=lf

# Denote all files that are truly binary and should not be modified.
*.eps binary
*.jpg binary
*.pdf binary
*.png binary
*.svg binary
